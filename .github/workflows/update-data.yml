name: Auto Update Scholar and GitHub Data

on:
  schedule:
    # Every Monday at midnight UTC
    - cron: '0 0 * * 1'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Update Script
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SCHOLAR_DELAY_MIN: '90'
          SCHOLAR_DELAY_MAX: '180'
          # Direct Scholar access from GitHub-hosted runners is often blocked.
          # Retry once with scholarly's public FreeProxies fallback.
          SCHOLAR_FREE_PROXY_FALLBACK: 'true'
        run: python update_data.py

      - name: Commit and Push Changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add data/scholar_github.json
          if ! git diff --cached --quiet; then
            git commit -m "Auto-update scholar and github data"
            git push
          else
            echo "No data changes to commit."
          fi
